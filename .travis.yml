language: python
python:
  - 3.5.2

#command to install dependencies
install: "pip install -r requirements.txt"

# command to run tests
script: py.test


# __5a after_success
after_success:
    - coveralls -v
